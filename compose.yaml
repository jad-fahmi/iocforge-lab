services:
  iocforge:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - iocforge-data:/home/iocforge/.local/share/iocforge-lab
    restart: unless-stopped

volumes:
  iocforge-data:
